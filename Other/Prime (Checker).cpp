#include <iostream>
using namespace std;

bool isPrime(int);

int main()
{
    int m, n, i;

    cin >> m >> n;

    for(i = m; i < n; i++)
    {
        if (isPrime)
            cout << " " << i << " ";

        else
            break;
    }

    bool isPrime(int N);

    {
        int N;

        if (N % 2 == 0)
            return false;
        else
            return true;
    }

    return 0;
}